This program is used to convert CNN models in the format of .mat to Binary file.
The program will read mat file produced by Matlab,so if you have to install matlab.
the direct dependencies is libmx.XX libmat.XX.